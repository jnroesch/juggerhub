## A streamlined .gitignore for modern .NET projects
## including temporary files, build results, and
## files generated by popular .NET tools. If you are
## developing with Visual Studio, the VS .gitignore
## https://github.com/github/gitignore/blob/main/VisualStudio.gitignore
## has more thorough IDE-specific entries.
##
## Get latest from https://github.com/github/gitignore/blob/main/Dotnet.gitignore

# Build results
[Dd]ebug/
[Dd]ebugPublic/
[Rr]elease/
[Rr]eleases/
x64/
x86/
[Ww][Ii][Nn]32/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/

# .NET Core
project.lock.json
project.fragment.lock.json
artifacts/

# ASP.NET Scaffolding
ScaffoldingReadMe.txt

# NuGet Packages
*.nupkg
# NuGet Symbol Packages
*.snupkg

# Others
~$*
*~
CodeCoverage/

# MSBuild Binary and Structured Log
*.binlog

# MSTest test Results
[Tt]est[Rr]esult*/
[Bb]uild[Ll]og.*

# NUnit
*.VisualState.xml
TestResult.xml
nunit-*.xml

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
dist/
build/

# Nx / Angular caches & test output
.nx/
.angular/
coverage/
test-output/
playwright-report/
blob-report/
.playwright/

# Environment files
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Docker
.docker/

# Claude Code / AI tooling
# Local, machine-specific Claude Code settings — keep per-machine
.claude/settings.local.json
# Git worktrees created by Claude Code. The rest of .claude/ IS tracked (settings.json,
# skills/), so without this a worktree lands inside a directory git watches and shows up as
# untracked noise in the main checkout. Prefer creating worktrees as siblings of the repo
# instead — nothing else that walks the tree from the root (graphify) honours .gitignore.
.claude/worktrees/
# Graphify generated knowledge-graph output
graphify-out/
# CI-only Playwright artifact dir (bind-mounted out of the e2e container)
playwright-artifacts/
# Raw GeoNames downloads used only to regenerate the city seed. Only the gzipped RESULT
# (cities500.seed.tsv.gz) is committed; cities500.txt alone is ~40 MB.
backend/Data/Seed/cities500.txt
backend/Data/Seed/cities500.zip
backend/Data/Seed/countryInfo.txt
backend/Data/Seed/admin1CodesASCII.txt
