#!/usr/bin/env bash
# Refresh the Graphify knowledge graph after a branch switch.
# Args: $1=prev_HEAD  $2=new_HEAD  $3=flag (1 = branch checkout, 0 = file checkout)
[ "${3:-0}" = "1" ] || exit 0
DIR="$(cd "$(dirname "$0")" && pwd)"
exec "$DIR/graphify-rebuild.sh"
