# Refuse to install under a Node/npm version outside the range declared in
# package.json "engines". Keeps every environment on the pinned Node line
# (see .nvmrc) instead of silently building on whatever Node happens to exist.
engine-strict=true
