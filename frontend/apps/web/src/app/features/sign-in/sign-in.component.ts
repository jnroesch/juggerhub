import { Component } from '@angular/core';

/**
 * Placeholder sign-in destination. The auth feature (sign-up / sign-in /
 * password reset) is deferred; the guard and interceptor route here so the
 * "redirect toward sign-in" behaviour is demonstrable in the walking skeleton.
 */
@Component({
  selector: 'jh-sign-in',
  imports: [],
  templateUrl: './sign-in.component.html',
  styleUrl: './sign-in.component.css',
})
export class SignInComponent {}
