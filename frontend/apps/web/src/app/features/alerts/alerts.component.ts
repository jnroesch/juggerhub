import { Component } from '@angular/core';

/**
 * Alerts / notifications (feature 008) — the destination exists and is reachable from the nav,
 * but the notifications system is a later feature. For now it shows a friendly, honest
 * placeholder; the nav shows no unread count.
 */
@Component({
  selector: 'jh-alerts',
  imports: [],
  templateUrl: './alerts.component.html',
  styleUrl: './alerts.component.css',
})
export class AlertsComponent {}
